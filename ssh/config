Host *
    UseKeychain yes

    # reuse existing sockets to speed up connections, e.g. with emacs remote
    # editing
    ControlMaster auto
    ControlPath ~/.ssh/sockets/%r@%h-%p
    ControlPersist 600


